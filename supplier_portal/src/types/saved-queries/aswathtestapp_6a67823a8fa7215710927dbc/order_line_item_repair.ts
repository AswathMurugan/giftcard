// AUTO-GENERATED by scripts/fetch-saved-queries.ts - do not edit by hand.
// Source: Phoenix /api/internal/component-definitions-all/saved-query
// Saved query: order_line_item_repair ("order_line_item_repair")
// Type: common_table_expression  |  Single output: true  |  App: aswathtestapp_6a67823a8fa7215710927dbc
// Rewrites order_line.item with a CLEAN JSON join key after card_spec_create_v2 (whose JSON-body substitution wraps every value in single quotes, corrupting item_rev_id). Params: orderLineId, itemRevId - both UUIDs, so they can never contain a comma and are immune to the comma-split defect. Deliberately writes ONLY {item_rev_id, status:'draft'}: name and status are read live from card_revs, so no free text goes into JSON. MUST be called with query-string params (NOT a JSON body) - query-string substitution writes clean JSON, JSON-body substitution quotes it.

import { apiManager } from '@/services/api-manager';
import { getDataHeaders } from '@/config/api-config';

/** Input parameters for `order_line_item_repair` (write — sent as a flat JSON body). */
export interface OrderLineItemRepairInput {
  itemRevId: string;
  orderLineId: string;
}

/** Row returned by `order_line_item_repair` (write — the affected row's id). */
export interface OrderLineItemRepairResult {
  id: string;
}

export const ORDER_LINE_ITEM_REPAIR_NAME = "order_line_item_repair";
export const ORDER_LINE_ITEM_REPAIR_IS_SINGLE = true as const;
export const ORDER_LINE_ITEM_REPAIR_APP_KEY = "aswathtestapp_6a67823a8fa7215710927dbc";

/**
 * Execute the `order_line_item_repair` saved query. WRITE (update).
 * Inputs are sent as a flat JSON body; returns the affected row(s).
 */
export async function executeOrderLineItemRepair(
  input: OrderLineItemRepairInput,
): Promise<OrderLineItemRepairResult[]> {
  const headers = getDataHeaders(ORDER_LINE_ITEM_REPAIR_APP_KEY || undefined);
  const response = await apiManager.post('data', "/saved-queries/order_line_item_repair/execute", input, headers);
  return response.data as OrderLineItemRepairResult[];
}
