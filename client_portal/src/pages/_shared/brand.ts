/**
 * The product name shown to users.
 *
 * Not `APPLICATION.label`: that is the platform's label for the app, and Vista
 * shares one app with Forge and Relay — the platform label cannot tell the
 * three apart. One constant, one name, and documents that leave the building
 * carry the same one.
 */
export const APP_BRAND = 'Vista';
